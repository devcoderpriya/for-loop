#include<stdio.h>
int main()
{
    int a;
    printf("enter the number by user :");
    scanf("%d",&a);

    int b;
    printf("enter the number by user :");
    scanf("%d",&b);


    int fact=1;
    int i;
  
   for (i=5; i >= b; i--)
    {
    fact =fact*i;
    }

    printf("%d",fact);
return 0;
}
