#include <stdio.h> 
int main() { 
    int num, a, lastDigit, firstDigit; 
    printf("Enter a number: "); 
    scanf("%d", &num); 
    a = num;  
    for (int i = 0; i < 1; i++) { 
        for (int j = 0; j < 1; j++) { 
            lastDigit = a % 10;   
            while (a >= 10) { 
                a = a / 10;   
            } 
f
 irstDigit = a;  
        } 
    } 
    printf("First Digit: %d\n", firstDigit); 
    printf("Last Digit: %d\n", lastDigit); 
    return 0;
